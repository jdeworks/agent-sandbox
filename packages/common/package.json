{
  "name": "@security-analyzer/common",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "lint": "eslint src --ext .ts",
    "format": "prettier --write src",
    "build": "tsc"
  },
  "dependencies": {
    "@security-analyzer/types": "*"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
