
## PHP Environment

PHP with common extensions (mbstring, xml, curl, zip, mysql, pgsql, sqlite3, bcmath, gd) and Composer are installed. Dependencies from `composer.json` are auto-installed on container startup.
