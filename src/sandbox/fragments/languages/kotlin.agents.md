
## Kotlin Environment

OpenJDK 21 is installed for running Kotlin/JVM projects. Most Kotlin projects use the Gradle wrapper (`./gradlew`) which bundles the Kotlin compiler. Dependencies are auto-resolved on container startup.
