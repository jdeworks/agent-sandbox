
## C# / .NET Environment

The .NET 8.0 SDK is installed. Use `dotnet` CLI for building, running, and managing packages. NuGet packages from project files (`.csproj`, `.fsproj`) are auto-restored on container startup.
