
## Dart Environment

The Dart SDK is installed at `/opt/dart-sdk`. Dependencies from `pubspec.yaml` are auto-fetched via `dart pub get` on container startup. The pub cache is persisted between sessions.
