
## Java Environment

OpenJDK 21 and Maven are installed system-wide. For Maven projects, dependencies from `pom.xml` are auto-resolved on container startup. Gradle wrapper (`./gradlew`) projects are also supported -- ensure the wrapper is executable.
